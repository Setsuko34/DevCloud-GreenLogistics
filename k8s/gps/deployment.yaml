apiVersion: apps/v1
kind: Deployment
metadata:
  name: gps
  namespace: app
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gps
  template:
    metadata:
      labels:
        app: gps
    spec:
      containers:
        - name: gps
          image: ghcr.io/setsuko34/greenlogistics-gps:52913b73340ac7aad6a9d74c330273cf776b10b2
          env:
            - name: REDPANDA_BROKERS
              value: redpanda.messaging.svc.cluster.local:9093
            - name: REDIS_URL
              value: redis://redis.app.svc.cluster.local:6379
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "64Mi"
              cpu: "100m"
