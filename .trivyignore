# GHSA-p6gq-j5cr-w38f — nodemailer raw option bypasses disableFileAccess/disableUrlAccess
# Not applicable: the notification service uses only createTransport() with plain SMTP config
# and sendMail() with text/from/to/subject. The `raw` option and disableFileAccess are never used.
GHSA-p6gq-j5cr-w38f
