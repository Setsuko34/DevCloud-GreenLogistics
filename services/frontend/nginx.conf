server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /parcels {
    proxy_pass http://api.app.svc.cluster.local:3000;
    proxy_set_header Host $host;
  }
}
